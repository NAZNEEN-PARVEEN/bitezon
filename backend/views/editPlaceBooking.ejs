<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Booking</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .booking-form-container {
      background-color: #fff;
      padding: 30px 40px;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 500px;
      margin: 60px auto;
    }

    .booking-form-container h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
    }

    .form-control {
      border-radius: 10px;
      padding: 12px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      transition: all 0.2s ease;
    }

    .form-control:focus {
      border-color: #007bff;
      box-shadow: 0 0 8px rgba(0,123,255,0.2);
    }

    .btn {
      border-radius: 12px;
      padding: 10px 18px;
      margin-right: 10px;
      transition: all 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-2px);
      opacity: 0.9;
    }

    .btn-group {
      text-align: center;
      display: flex;
      justify-content: center;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <%- include('navbar/nav.ejs') %>
  <br><br><br>

  <div class="booking-form-container">
    <h2>Edit Booking for "<%= recipe.recipeName %>"</h2>

    <form action="/place/edit/<%= recipe._id %>/<%= booking._id %>?_method=PUT" method="POST">
      <!-- Username readonly -->
      <input type="text" name="username" class="form-control" value="<%= booking.username %>" readonly required>

      <input type="text" name="placeName" class="form-control" value="<%= booking.placeName %>" placeholder="Enter place name" required>
      <input type="number" name="personCount" class="form-control" value="<%= booking.personCount %>" placeholder="Number of persons" required>
      <input type="number" name="days" class="form-control" value="<%= booking.days %>" placeholder="Number of days" required>

      <!-- Meals input -->
      <input type="text" name="meals" class="form-control" value="<%= booking.meals.join(', ') %>" placeholder="Meals (comma separated, e.g., Breakfast, Lunch)" required>

      <textarea name="address" class="form-control" placeholder="Full address including city & zip" required><%= booking.address %></textarea>

      <div class="btn-group mt-3">
        <button type="submit" class="btn btn-success">Update Booking</button>
        <a href="/recipe/show/<%= recipe._id %>" class="btn btn-secondary">Cancel</a>
      </div>
    </form>
  </div>

  <%- include('chatbot') %>
  <br><br><br><br>

  <%- include('navbar/footer.ejs') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
