<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Travel Booking Form</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(to right, #d9a7c7, #fffcdc);
      font-family: 'Poppins', sans-serif;
      color: #333;
      min-height: 100vh;
    }

    .booking-container {
      max-width: 700px;
      background: #fff;
      border-radius: 20px;
      padding: 40px;
      margin: 60px auto;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }

    h2 {
      text-align: center;
      margin-bottom: 25px;
      font-weight: 600;
      color: #4a148c;
    }

    .form-control, .form-select, textarea {
      border-radius: 10px;
      padding: 12px;
    }

    label {
      font-weight: 500;
      margin-top: 10px;
    }

    .btn-submit {
      background: linear-gradient(135deg, #6a11cb, #2575fc);
      border: none;
      color: white;
      font-weight: 500;
      border-radius: 10px;
      padding: 12px;
      width: 100%;
      transition: 0.3s;
    }

    .btn-submit:hover {
      background: linear-gradient(135deg, #2575fc, #6a11cb);
      transform: scale(1.02);
    }

    .meal-options label {
      margin-right: 15px;
    }

    hr {
      margin: 0;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <%- include('navbar/nav.ejs') %>


  <!-- Booking Form -->
  <div class="booking-container">
    <h2>Book Your Place</h2>
    <form action="/place/add" method="POST">
      <input type="hidden" name="recipeId" value="<%= recipe._id %>">

      <div class="mb-3">
        <label>Your Name</label>
       <input type="text" class="form-control" name="username"
  value="<%= user ? user.name : (booking ? booking.username : '') %>"
  readonly>

      </div>

      <div class="mb-3">
        <label>Place Name / Location</label>
        <input type="text" class="form-control" name="placeName" placeholder="Enter place name" required>
      </div>

      <div class="row">
        <div class="col-md-6 mb-3">
          <label>Number of Persons</label>
          <input type="number" class="form-control" name="personCount" min="1" placeholder="e.g. 2" required>
        </div>
        <div class="col-md-6 mb-3">
          <label>Number of Days / Nights</label>
          <input type="number" class="form-control" name="days" min="1" placeholder="e.g. 3" required>
        </div>
      </div>

      <div class="mb-3 meal-options">
        <label>Meals Included:</label><br>
        <div class="form-check form-check-inline">
          <input type="checkbox" class="form-check-input" name="meals" value="Breakfast" id="meal1">
          <label class="form-check-label" for="meal1">Breakfast</label>
        </div>
        <div class="form-check form-check-inline">
          <input type="checkbox" class="form-check-input" name="meals" value="Lunch" id="meal2">
          <label class="form-check-label" for="meal2">Lunch</label>
        </div>
        <div class="form-check form-check-inline">
          <input type="checkbox" class="form-check-input" name="meals" value="Dinner" id="meal3">
          <label class="form-check-label" for="meal3">Dinner</label>
        </div>
      </div>

      <div class="mb-3">
        <label>Address</label>
        <textarea class="form-control" name="address" rows="3" placeholder="Enter your address" required></textarea>
      </div>

      <div class="mb-3">
        <label>Booking Date</label>
        <input type="date" class="form-control" name="bookingDate" required>
      </div>

      <button type="submit" class="btn btn-submit">Book Now</button>
    </form>
  </div>

   </div>

   <br><br>
   <br>  <%- include('chatbot') %>
  <!-- Footer -->
  <%- include('navbar/footer.ejs') %>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
