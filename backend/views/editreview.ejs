<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Review</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="">
    <!-- Navbar -->
  <%- include('navbar/nav.ejs') %>
  
  <br><br><br> <br><br>


  <div class="container mt-5" style="max-width: 500px;">
    <h3 class="text-center mb-4">Edit Review for "<%= recipe.recipeName %>"</h3>
    
    <form action="/recipes/<%= recipe._id %>/reviews/<%= review._id %>/edit" method="POST">
      <div class="mb-3">
        <input type="text" name="comment" class="form-control" value="<%= review.comment %>" placeholder="Edit your comment" required>
      </div>
      <div class="mb-3">
        <select name="rating" class="form-control" required>
          <option value="">Select rating</option>
          <option value="1" <%= review.rating === 1 ? "selected" : "" %>>1 ★</option>
          <option value="2" <%= review.rating === 2 ? "selected" : "" %>>2 ★★</option>
          <option value="3" <%= review.rating === 3 ? "selected" : "" %>>3 ★★★</option>
          <option value="4" <%= review.rating === 4 ? "selected" : "" %>>4 ★★★★</option>
          <option value="5" <%= review.rating === 5 ? "selected" : "" %>>5 ★★★★★</option>
        </select>
      </div>
      <button type="submit" class="btn btn-success">Update Review</button>
      <a href="/recipe/<%= recipe._id %>" class="btn btn-secondary">Cancel</a>
    </form>
  </div>

  
  <%- include('chatbot') %>

  <br><br><br><br><br><br>

  <!-- Footer -->
  <%- include('navbar/footer.ejs') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
